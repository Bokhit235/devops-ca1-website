apiVersion: v1
kind: Service
metadata:
  name: devops-ca1-website-service
spec:
  type: NodePort
  selector:
    app: devops-ca1-website
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001